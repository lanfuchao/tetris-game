# 快速开始指南 🚀

## 5 秒开始游戏

```bash
cd /Users/eric/work/games/tetris
./start.sh
```

游戏将自动在浏览器中打开！

## 常用命令

```bash
# 开发
npm run dev          # 启动开发服务器

# 构建
npm run build        # 构建生产版本
npm run clean        # 清理构建文件

# 脚本
./start.sh           # 一键启动（推荐新手）
./deploy.sh          # 构建并部署
```

## 难度选择

游戏提供三种难度，**开始前请先选择难度**：

| 难度 | 速度 | 分数倍率 | 适合人群 |
|------|------|---------|---------|
| 简单 | 慢 | 1.0x | 新手 |
| 普通 | 中等 | 1.5x | 普通玩家 |
| 困难 | 快 | 2.0x | 高手 |

**注意**: 游戏开始后无法更改难度，需要重新开始才能切换。

## 游戏操作

| 按键 | 功能 |
|------|------|
| ← | 左移 |
| → | 右移 |
| ↑ | 旋转 |
| ↓ (按住) | 快速下落 |
| 空格 | 瞬间落地 |
| P | 暂停/继续 |

## 目录说明

```
src/
├── game.ts         ← 游戏核心逻辑（修改游戏规则从这里开始）
├── config.ts       ← 配置文件（颜色、形状、尺寸）
├── utils/
│   ├── audio.ts    ← 音效系统
│   └── renderer.ts ← 渲染引擎
└── styles/
    └── main.css    ← 样式（修改外观从这里开始）
```

## 自定义游戏

### 修改方块颜色
编辑 `src/config.ts`:
```typescript
colors: [
    null,
    '#FF0D72',  // 方块1的颜色
    '#0DC2FF',  // 方块2的颜色
    // ...
]
```

### 调整难度参数
编辑 `src/difficulty.ts`:
```typescript
[Difficulty.NORMAL]: {
    initialSpeed: 800,        // 初始速度
    speedCurve: (level: number) => {
        const speed = 800 * Math.pow(0.88, level - 1);  // 衰减率
        return Math.max(120, Math.round(speed));        // 最低速度
    },
    scoreMultiplier: 1.5,     // 分数倍率
    levelUpThreshold: 1000    // 升级门槛
}
```

### 修改得分规则
编辑 `src/game.ts`，找到 `clearLines()` 方法:
```typescript
const points = [0, 100, 300, 500, 800]; // 修改这些数值
```

## 部署到线上

### GitHub Pages (免费)
1. 推送代码到 GitHub
2. 启用 GitHub Pages
3. GitHub Actions 会自动构建部署

### Vercel (推荐，免费)
```bash
npm install -g vercel
vercel
```

### Netlify (免费)
```bash
npm run build
# 将 dist/ 目录拖到 Netlify 网站
```

## 常见问题

### Q: 音效不工作？
A: 某些浏览器需要用户交互后才能播放音效，点击"开始"按钮即可。

### Q: 构建失败？
A: 删除 `node_modules` 和 `package-lock.json`，重新运行 `npm install`。

### Q: 热重载不工作？
A: 检查端口 8080 是否被占用，或修改 `webpack.config.js` 中的 `devServer.port`。

## 技术栈

- TypeScript 5.3
- Webpack 5
- HTML5 Canvas
- Web Audio API

## 获取帮助

- 查看 `README.md` 了解详细文档
- 查看 `PROJECT_STRUCTURE.md` 了解架构设计
- 提交 Issue 报告问题

## 下一步

1. ✅ 运行游戏
2. 🎨 自定义颜色和样式
3. 🎮 调整游戏难度
4. 🚀 部署到线上
5. 📱 添加移动端支持
6. 🏆 实现排行榜功能

祝你玩得开心! 🎉
